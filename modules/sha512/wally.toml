[package]
name = "dekkonot/sha512"
description = "Implementation of the SHA-512 hashing algorithm"
license = "MIT"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

version = "1.0.0"

include = ["*.luau", "wally.toml"]
exclude = ["*"]

[dependencies]
